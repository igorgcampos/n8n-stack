# Arquivo .env — carregado automaticamente pelo Docker Compose para
# interpolação de variáveis no docker-compose.yml (ex: ${REDIS_PASSWORD}).
# NÃO é o mesmo que env_file de container.

REDIS_PASSWORD=DEFINA_UMA_SENHA_FORTE  # openssl rand -hex 24 (igual ao QUEUE_BULL_REDIS_PASSWORD do n8n.env)
